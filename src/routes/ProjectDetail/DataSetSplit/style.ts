import { customSx } from "styles/utils";

export const splitDataWrapperSx = customSx({
  display: "flex",
  position: "absolute",
  top: 0,
  left: 0,
  py: "13px",
  height: "4px",
  width: "100%",
});

export const generalSlideDataSx = customSx({
  alignItems: "center",
  height: 4,
});
